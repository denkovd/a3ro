# Finnhub API Verification Report

**Date:** 2026-07-16T09:59:57.311Z

---

## V1: Calendar Earnings History Depth

**Requirement:** Verify how far back `/calendar/earnings?symbol=X` returns on the free tier (request 3 years; is it truncated?).

**Why it matters:** Sets backfill depth → streak depth.

**Status:** ❌ FAIL

**Finding:** History is truncated or endpoints returned errors.

**Details:**
```json
{
  "NVDA": {
    "quartersWithActual": 0,
    "totalRows": 0,
    "oldestDate": "N/A",
    "dateRange": "2023-07-16 to 2026-07-16"
  },
  "AAPL": {
    "quartersWithActual": 0,
    "totalRows": 0,
    "oldestDate": "N/A",
    "dateRange": "2023-07-16 to 2026-07-16"
  },
  "ORCL": {
    "quartersWithActual": 0,
    "totalRows": 0,
    "oldestDate": "N/A",
    "dateRange": "2023-07-16 to 2026-07-16"
  }
}
```

**Sample Calendar Payloads (first 2 entries per ticker):**
```json
{
  "NVDA": [],
  "AAPL": [],
  "ORCL": []
}
```

---

## V2: Year/Quarter Label Agreement

**Requirement:** Verify whether `(year, quarter)` labels **agree between the two endpoints** for offset-fiscal-year companies (test NVDA, AAPL, ORCL).

**Why it matters:** Flow B joins the endpoints on `(year, quarter)`. If labels disagree (fiscal vs calendar labeling), enrichment lands on the wrong row.

**Status:** ❌ FAIL

**Finding:** Labels disagree for one or more tickers (see mismatches below).

**Details:**
```json
{
  "NVDA": {
    "calendarQuarters": 0,
    "stockQuarters": 4,
    "agree": false,
    "mismatchesSample": [
      {
        "inStockNotCalendar": "2027-Q1"
      },
      {
        "inStockNotCalendar": "2026-Q4"
      },
      {
        "inStockNotCalendar": "2026-Q3"
      }
    ]
  },
  "AAPL": {
    "calendarQuarters": 0,
    "stockQuarters": 4,
    "agree": false,
    "mismatchesSample": [
      {
        "inStockNotCalendar": "2026-Q2"
      },
      {
        "inStockNotCalendar": "202
  ... [truncated]
```

---

## V3: Calendar Hour Field Values

**Requirement:** Verify the actual value set of calendar `hour` (docs say `bmo`/`amc`/`dmh`; empty string has been observed).

**Why it matters:** A CHECK constraint that rejects `''` aborts inserts. Fallback: pipeline normalizes `'' → NULL` (§2.1).

**Status:** ✅ PASS

**Finding:** Only expected hour values observed (bmo, amc, dmh, or empty).

**Details:**
```json
{
  "distinctValues": [],
  "counts": {},
  "expectedValues": [
    "bmo",
    "amc",
    "dmh",
    ""
  ],
  "unexpectedValues": [],
  "note": "Empty string is normalized to NULL in pipeline (per spec §2.1)"
}
```

---

## Summary

| Verification | Status | Fallback |
|---|---|---|
| **V1** | ❌ FAIL | Backfill EPS-only older quarters from /stock/earnings if history too short |
| **V2** | ❌ FAIL | Drop the join; skip /stock/earnings enrichment entirely if labels disagree |
| **V3** | ✅ PASS | Already handled: pipeline normalizes empty string → NULL |

---

*Generated by `backend/scripts/verify-earnings-endpoints.ts`*
